#include<iostream>
#include<math.h>
#include<studio.h>

using namespace std;
int main()
{
    double,a,b,c,x;
    cin>>a>>b>>c>>x;
    double A = 2*x*x*+pow(a+x*x),1/3;
    double B a*a+b*c;
    double C = log b+ln c*c;
    double D = pow(cos(a+pow(x,3)),2);
    double Z = A/B+c/d
}
